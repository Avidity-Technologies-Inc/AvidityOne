version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    # Dependency upgrades are reviewed manually under the main-only workflow.
    # Security PR automation is separately disabled in repository settings; alerts stay enabled.
    open-pull-requests-limit: 0
